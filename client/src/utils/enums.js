const MODE_COLOR = Object.freeze({
    win: '#52c41a',
    loss: '#ff4d4f',
    tie: '#ccc',
})

const MODE = Object.freeze({
    WIN: 'win',
    LOSS: 'loss',
    TIE: 'tie',
})

const SEARCH = Object.freeze({
    NAME: 'name',
    SCORE: 'score',
})

module.exports = {
    MODE_COLOR,
    MODE,
    SEARCH
}